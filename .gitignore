node_modules/
out/
dist/
runtime/
kadr-fragments-seed/
*.log
.claude/
*.autosave.kadr
node_modules

# ttsqc: рабочие каталоги пакета и кэши, не исходники
python/cache/
python/runs/
python/**/__pycache__/
scripts/__pycache__/
python/models/lexicon_learned.json

# Дневник исследования детектора — личные записи автора (пути и названия
# проектов). Код его не читает; в репозитории ему делать нечего.
python/NOTES-ttsqc.md

# Резервные копии модели, которые «Переобучить» кладёт рядом при каждом прогоне
python/models/scorer.*.pkl
